<IfModule mod_rewrite.c>
    RewriteEngine On

    # Set base to root
    RewriteBase /

    # If request is not a real file or folder, route to index.php
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^ index.php [L]
</IfModule>
